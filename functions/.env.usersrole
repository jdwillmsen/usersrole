# Real-project deploy config. Empty allow-list keeps sign-up closed on the
# retiring project; the value is required so a non-interactive deploy resolves
# the SIGNUP_ALLOWLIST param instead of prompting.
SIGNUP_ALLOWLIST=
