rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // users/{uid} holds only that user's theme preferences.
    match /users/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
